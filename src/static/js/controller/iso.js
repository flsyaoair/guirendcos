dashboard.controller('isoList', ['$scope', function($scope){

	$scope.init = function () {
		// App.init();
	}
}])

dashboard.controller('isoMake', ['$scope', function($scope){

	$scope.init = function () {
		// App.init();
	}
}])